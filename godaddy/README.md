# godaddy

This folder is a quarantine area for raw GoDaddy-derived source material and legacy rebuild helpers.

Rules:
- Reference-only for AI agents.
- Do not import from here in runtime code.
- Do not add new functional application code here.
- Do not modify files here unless you are explicitly working on archival or forensic cleanup.

What lives here:
- Raw scraped HTML exports
- Raw exported CSS
- Legacy site-media manifest and rebuild helpers

The live application should use the standalone copies under `content/`, `app/`, `lib/`, and `public/`.
